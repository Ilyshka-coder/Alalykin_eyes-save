<Window x:Class="Алалыкин_Глазки_save.ChangePriority"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Алалыкин_Глазки_save"
        mc:Ignorable="d"
        Title="ChangePriority" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Введите новое значение приоритета:" FontSize="14" Margin="0,0,0,10"/>

        <TextBox x:Name="PriorityTextBox" 
                 Grid.Row="1" 
                 FontSize="16" 
                 Padding="5"
                 PreviewTextInput="PriorityTextBox_PreviewTextInput"/>

        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,20,0,0">
            <Button Content="Изменить" 
                    Width="100" 
                    Height="35" 
                    Margin="0,0,10,0"
                    Click="ChangeButton_Click"
                    IsDefault="True"/>
            <Button Content="Отмена" 
                    Width="100" 
                    Height="35"
                    Click="CancelButton_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
